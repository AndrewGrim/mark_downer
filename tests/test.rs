//use markdowner;
// TODO Once the basics are complete this will test
// all supported elements in one file using the public api.
// There will be another test which might need an --ignore attribute
// it will test the same things but repeated to check performance
// on large inputs.